# {{ ansible_managed }}

allow-hotplug l3roam0
auto l3roam0
iface l3roam0 inet manual
	up ip link set $IFACE up
	post-up ip -6 a add {{ ipv6_local.address }} dev lo
	post-up ip -6 r add {{ ipv6_local_network }} dev $IFACE t 252
