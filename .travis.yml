---

language: python
python: "2.7"

before_install:
- sudo apt-get update -qq
- sudo apt-get install -qq python-apt openssh-server

install:
- pip install ansible==1.9.3 ipcalc

script:
- ansible-playbook -v -i tests/inventory tests/test.yml --connection=local --sudo
